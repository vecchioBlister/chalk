del <var>
	deletes <var>.

You can delete multiple <var> at the same time, separeted by whitespaces.

You can delete all variables by typing 'del *'

If the deleted <var> was manipulated, 'ans' gets now manipulated.