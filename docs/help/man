man <var>
	selects *<var> for manipulation

By default, the manipulated variable is 'ans'.
If no argument is given, 'ans' is selected for manipulation.

When manipulating a variable, you can operate on it without explicitly referring to it.